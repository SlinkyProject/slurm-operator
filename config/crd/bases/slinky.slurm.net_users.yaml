---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: users.slinky.slurm.net
spec:
  group: slinky.slurm.net
  names:
    kind: User
    listKind: UserList
    plural: users
    shortNames:
    - slurmuser
    singular: user
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The Slurm user name.
      jsonPath: .spec.userName
      name: USER
      type: string
    - description: The user's default account.
      jsonPath: .spec.defaultAccount
      name: DEFAULT ACCOUNT
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: User is the Schema for the users API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UserSpec defines the desired state of User.
            properties:
              adminLevel:
                default: None
                description: adminLevel granted to the user.
                enum:
                - None
                - Operator
                - Administrator
                type: string
              associations:
                description: associations are the account memberships for this user.
                items:
                  description: UserAssociation is a single account membership for
                    a User.
                  properties:
                    account:
                      description: account name this user belongs to.
                      type: string
                    limits:
                      description: limits for this specific membership.
                      properties:
                        defaultQos:
                          description: DefaultQOS for this association; must be present
                            in QOS.
                          type: string
                        fairshare:
                          description: Fairshare is an integer number of shares, or
                            the literal "parent".
                          type: string
                        grpJobs:
                          format: int32
                          type: integer
                        grpSubmitJobs:
                          format: int32
                          type: integer
                        grpTRES:
                          additionalProperties:
                            type: string
                          description: GrpTRES is the aggregate TRES limit, e.g. {"cpu":"10","gres/gpu":"4"}.
                          type: object
                        grpWall:
                          type: string
                        maxJobs:
                          format: int32
                          type: integer
                        maxSubmitJobs:
                          format: int32
                          type: integer
                        maxTRESPerJob:
                          additionalProperties:
                            type: string
                          description: MaxTRESPerJob is the per-job TRES limit, e.g.
                            {"cpu":"4"}.
                          type: object
                        maxWallPerJob:
                          type: string
                        priority:
                          description: Priority added to jobs using this association.
                          format: int32
                          type: integer
                        qos:
                          description: QOS is the list of QOS names allowed for this
                            association.
                          items:
                            type: string
                          type: array
                      type: object
                    partition:
                      description: partition scopes this association to a Slurm partition.
                      type: string
                  required:
                  - account
                  type: object
                minItems: 1
                type: array
              controllerRef:
                description: controllerRef references the target Controller CR.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              defaultAccount:
                description: defaultAccount must match one of the associations' account.
                type: string
              deletionPolicy:
                default: Delete
                description: deletionPolicy controls the Slurm-side entity on CR deletion.
                enum:
                - Delete
                - Orphan
                type: string
              userName:
                description: userName in Slurm; defaults to metadata.name if empty.
                type: string
            required:
            - associations
            - controllerRef
            - defaultAccount
            type: object
            x-kubernetes-validations:
            - message: defaultAccount must match one of the associations
              rule: self.associations.exists(a, a.account == self.defaultAccount)
          status:
            description: UserStatus defines the observed state of User.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
